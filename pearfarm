#!/usr/bin/env php
<?php

if (strpos('@php_bin@', '@php_bin') === 0) {  // not a pear install
    require_once dirname(__FILE__).DIRECTORY_SEPARATOR.'pearfarm-cli.php';
} else {
    require 'pearfarm'.DIRECTORY_SEPARATOR.'pearfarm-cli.php';
}
